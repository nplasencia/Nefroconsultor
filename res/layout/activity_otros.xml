<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_main_bitmap"
    tools:context="com.madilon.nefroconsultor.ui.OtrosActivity" >

    <TextView
        android:id="@+id/cabecera"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/title_activity_otros"
        android:textColor="@color/windowsBackground"
        android:textSize="@dimen/btnText"
        android:background="@color/titleTextColor"
        android:gravity="center"
        android:paddingTop="3dp"
        android:paddingBottom="3dp"/>
    
    <LinearLayout
        android:id="@+id/layout_btn_calcular"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical|center_horizontal"
        android:orientation="horizontal"
        android:layout_alignParentBottom="true"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:layout_marginRight="@dimen/activity_horizontal_margin"
        android:layout_marginBottom="@dimen/activity_vertical_margin">
	        
	    <Button 
	        android:id="@+id/btn_calcular"
	        android:layout_width="fill_parent"
	        android:layout_height="40dp"
	        android:text="@string/calcular"
	        android:shadowColor="#000000"
	        android:background="@drawable/btn_calcular"
	        android:textColor="@color/btnCalcularText"
	        android:textSize="@dimen/btnText"/>
	</LinearLayout>
	   
    <ListView
        android:id="@+id/list"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/layout_btn_calcular"
        android:layout_below="@id/cabecera"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:layout_marginRight="@dimen/activity_horizontal_margin"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        android:background="@color/transparent"
        android:divider="@null"
        android:dividerHeight="5dp"
        android:footerDividersEnabled="false"
        android:headerDividersEnabled="false"/>

</RelativeLayout>
